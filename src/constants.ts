export let REFERENCE_REGEX = /\[\u2197\]\(urn:([^)]*)\)/g;

export let ACTION_TYPE = {
	MOUSE: "mouse",
	CURSOR: "cursor",
	BACKLINK: "backlink",
};

// 8203 is a zero-width space character
export const ZERO_WIDTH_SPACE_CODE = 8203;
